let empJSON={
              "name" : "dinga",
              "age" : 24,
              "hobbies" : ['dancing','singing']
            }
console.log(empJSON);
let JSONObject=JSON.stringify(empJSON);//to convert js object to string
console.log(JSONObject);
let jsobject= JSON.parse(JSONObject);//to convert string to js object











/* console.log(`MY NAME IS ${empJSON.name} `);
 string interpulation that is supported only fa back ticks or tilt
 multi 
line comment short cut "alt+shift+a"  */